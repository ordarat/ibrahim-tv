name: kurdish_tv_app
description: A new Flutter project for Kurdish TV Web App.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  carousel_slider: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  # لێرەدا ڕێڕەوی وێنەکانمان ناساندووە بە ئەپەکە
  assets:
    - assets/logo.png
